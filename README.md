# Bajaj-Finserv-Health
Developed a Java-based system to integrate with external APIs, perform data deduplication, aggregate participant scores, and generate a validated leaderboard with accurate total computation.
